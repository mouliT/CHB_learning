% ============================================================
%  DC-Link Voltage Balancing Modulation for Cascaded H-Bridge Converters
%  Paper 3 Reference Notes
%  Source: Ko, Tcai, Liserre, IEEE Access 2021
%  Figures stored in: figures/
%  Naming convention: DLVBMCHBC_Fig[N]_page[XX].png  (caption-extracted at 200 DPI)
% ============================================================
\documentclass[12pt, a4paper]{article}

% ---- core packages ----
\usepackage[utf8]{inputenc}
\usepackage[T1]{fontenc}
\usepackage{amsmath, amssymb, amsthm}
\usepackage{bm}
\usepackage{siunitx}

% ---- figures ----
\usepackage{graphicx}
\graphicspath{{figures/}}
\usepackage{float}
\usepackage{caption}
\usepackage{subcaption}

% ---- layout ----
\usepackage[a4paper, top=2.5cm, bottom=2.5cm, left=3cm, right=2.8cm]{geometry}
\usepackage{parskip}
\setlength{\parskip}{6pt}
\setlength{\parindent}{0pt}

% ---- hyperlinks ----
\usepackage[colorlinks=true, linkcolor=blue!60!black, citecolor=blue!50!black]{hyperref}
\usepackage{bookmark}
\makeatletter
\pdfstringdefDisableCommands{%
    \def\vspace#1{}%
    \def\rule#1#2{}%
    \def\color#1{}%
    \let\leavevmode@ifvmode\relax%
    \def\,{ }%
}
\makeatother

% ---- styled boxes ----
\usepackage{tcolorbox}
\tcbuselibrary{skins, breakable}

% ---- tables ----
\usepackage{booktabs}
\usepackage{array}
\usepackage{longtable}

% ---- section formatting ----
\usepackage{titlesec}
\usepackage{xcolor}
\definecolor{chbblue}{RGB}{10, 55, 120}
\definecolor{chborange}{RGB}{190, 70, 5}
\definecolor{chbgreen}{RGB}{8, 95, 38}
\definecolor{chbred}{RGB}{170, 25, 25}
\definecolor{lightblue}{RGB}{216, 232, 252}
\definecolor{lightorange}{RGB}{255, 244, 220}
\definecolor{lightgreen}{RGB}{216, 247, 228}
\definecolor{lightred}{RGB}{255, 228, 228}
\definecolor{lightgray}{RGB}{245, 245, 245}

\titleformat{\section}{\large\bfseries\color{chbblue}}{{\thesection.}}{0.5em}{}
            [\vspace{-4pt}\rule{\textwidth}{0.5pt}]
\titleformat{\subsection}{\normalsize\bfseries\color{chbblue!80!black}}{{\thesubsection}}{0.5em}{}

% ---- tcolorbox styles ----
\newtcolorbox{circuitbox}[1][Circuit Description]{
    colback=lightblue, colframe=chbblue, coltitle=white,
    fonttitle=\bfseries\small, title={#1},
    breakable, enhanced, left=4pt, right=4pt, top=4pt, bottom=4pt}

\newtcolorbox{statebox}[1][State Variables \& Parameters]{
    colback=lightorange, colframe=chborange, coltitle=white,
    fonttitle=\bfseries\small, title={#1},
    breakable, enhanced, left=4pt, right=4pt, top=4pt, bottom=4pt}

\newtcolorbox{eqnbox}[1][Governing Equations]{
    colback=lightgreen, colframe=chbgreen, coltitle=white,
    fonttitle=\bfseries\small, title={#1},
    breakable, enhanced, left=4pt, right=4pt, top=4pt, bottom=4pt}

\newtcolorbox{physbox}[1][Physical Interpretation]{
    colback=lightgray, colframe=gray!60, coltitle=white,
    fonttitle=\bfseries\small, title={#1},
    breakable, enhanced, left=4pt, right=4pt, top=4pt, bottom=4pt}

\newtcolorbox{warningbox}[1][Watch Out]{
    colback=lightred, colframe=chbred, coltitle=white,
    fonttitle=\bfseries\small, title={#1},
    breakable, enhanced, left=4pt, right=4pt, top=4pt, bottom=4pt}

% ---- custom commands ----
\newcommand{\vg}{v_{g}}
\newcommand{\Vg}{V_{g}}
\newcommand{\ig}{i_{g}}
\newcommand{\Ig}{I_{g}}
\newcommand{\omg}{\omega}
\newcommand{\phig}{\phi_{g}}
\newcommand{\Vdc}{V_{dc}}
\newcommand{\Vdci}[1]{V_{dc,#1\mathrm{H}}}
\newcommand{\Vtot}{V_{dc,\mathrm{tot}}}
\newcommand{\Mi}{M_{i}}
\newcommand{\phic}{\varphi}
\newcommand{\vref}{v_{\mathrm{ref}}}
\newcommand{\PH}{P_{H}}
\newcommand{\PHi}[1]{P_{#1\mathrm{H}}}
\newcommand{\PHnc}{P_{H}^{\mathrm{nc}}}
\newcommand{\PHcl}{P_{H}^{\mathrm{cl}}}
\newcommand{\figref}[1]{Figure~\ref{#1}}

% ---- figure entry macro ----
\newcommand{\figentry}[4]{%
    \begin{figure}[H]
        \centering
        \includegraphics[width=#4\textwidth]{#2}
        \caption{#3}
        \label{#1}
    \end{figure}%
}

% ============================================================
\title{\textbf{DC-Link Voltage Balancing Modulation}\\[4pt]
       \textbf{for Cascaded H-Bridge Converters}\\[6pt]
       \large A Self-Contained Tutorial\\[4pt]
       \normalsize Ko, Tcai, Liserre --- IEEE Access 2021}
\date{\today}
% ============================================================

\begin{document}
\maketitle
\tableofcontents
\clearpage

% ============================================================
\section{The CHB Converter: Circuit, Cascading, and the Balancing Problem}
\label{sec:circuit}
% ============================================================

\textit{This section builds the CHB from a single cell upward, shows why cascading
gives a better waveform, and identifies the core balancing problem that the paper
solves.}

The paper studies a
\textbf{grid-connected cascaded H-bridge (CHB)} converter where each cell has an
\textbf{isolated DC-link capacitor} and its own DC source.
Because different sources can deliver different power, the cell voltages naturally
drift apart unless the controller actively redistributes how much \emph{AC-side power}
each cell processes.

% ── §1.1 ─────────────────────────────────────────────────────────────────
\subsection{From One Cell to Many: How Cascading Works}
\label{subsec:cascading}

An H-bridge cell has two ports.
On the \textbf{DC side}, an isolated capacitor at voltage $\Vdci{i}$ is fed
by a private source (a PV string, battery, or rectifier) delivering power
$P_{i,\mathrm{src}}$.
On the \textbf{AC side}, four transistors chop that DC voltage at carrier
frequency $f_c \gg f$.
Averaged over one switching period, the fundamental component of the AC
output is:
\[
    \bar{v}_i(t) = \Vdci{i}\,\Mi\,\sin(\omg t)
\]
where $\Mi \in [0,1]$ is the modulation index --- the single knob the
controller turns to set how much power this cell contributes.

In reality the instantaneous $v_i(t)$ is a high-frequency PWM waveform; in this
tutorial we use the fundamental component because it is what transfers power to
the grid at frequency $f$.

Cascading means connecting the AC output terminals of $n$ cells in
\emph{series}.
The same grid current $\ig$ is forced through every cell.
The total voltage seen by the grid is:
\[
    \vg(t) = v_1(t) + v_2(t) + \cdots + v_n(t)
\]

\figentry{fig:staircase_buildup}{CHB_staircase_buildup}%
    {How three individual H-bridge cell outputs combine to form a
     seven-level multilevel waveform ($n = 3$, $f_c = 12f$, $M = 0.95$).
     \textbf{Rows 1--3:} each cell's unipolar PWM output (solid colour)
     with its phase-shifted triangular carrier (grey) and the shared
     sinusoidal reference (dashed).
     \textbf{Row 4 (Sum):} the series sum $\vg = v_1 + v_2 + v_3$
     steps through seven discrete levels and closely tracks the ideal
     sinusoid $3\Vdc M\sin(\omg t)$ (red dashed).
     The orange shading is the residual instantaneous error; phase-shifted
     carriers ensure no two cells switch simultaneously.}{1.0}

\figentry{fig:staircase_summation}{CHB_staircase_summation}%
    {Effect of increasing the number of cells $n = 1, 2, 3, 4$ on output
     waveform quality ($f_c = 6f$, $M = 0.95$).
     Each row shows the total output $\vg$ on a normalised axis
     $(\vg / n\Vdc)$.
     With $n = 1$ the output has only 3 levels and large harmonic error
     (orange shading).
     Each additional cell doubles the number of levels
     (3 $\to$ 5 $\to$ 7 $\to$ 9), shrinking the error region visibly.
     This is why CHB converters dominate high-power applications:
     more cells means lower harmonic distortion and a smaller filter $L_f$
     with no increase in individual transistor voltage rating.}{1.0}

% ── §1.2 ─────────────────────────────────────────────────────────────────
\subsection{Full Circuit Topology}
\label{subsec:topology}

\figentry{fig:paper_fig1}{DLVBMCHBC_Fig1_page01}%
    {Grid-connected CHB converter with unbalanced loading powers
     $\PHi{1} \neq \PHi{2} \neq \PHi{3}$ (paper Fig.\,1).
     Each H-bridge cell has its own isolated DC-link capacitor fed by an
     independent DC source.
     The AC output terminals of all $n$ cells are connected in series;
     the series string drives the grid through filter inductance $L_f$.}{0.75}

\begin{circuitbox}[Component Walk-through (paper Fig.\,1)]
\begin{enumerate}
    \item \textbf{DC sources (right side of each cell).}
          Each cell $i$ has a private DC source delivering $P_{i,\mathrm{src}}$
          to its isolated capacitor at $\Vdci{i}$.
          The dashed isolation boundary is not decorative:
          there is \emph{no} electrical connection between DC sides of
          different cells.

    \item \textbf{H-bridge switches (centre of each cell).}
          Four transistors per cell chop the DC voltage at carrier
          frequency $f_c$.
          The average AC output is
          $\bar{v}_i = \Vdci{i}\,\Mi\,\sin(\omg t)$.

    \item \textbf{AC series string (left column).}
          The top terminal of cell~$i$ connects to the bottom terminal
          of cell~$(i{+}1)$.
          Total output: $\vg = v_1 + \cdots + v_n$.

    \item \textbf{Filter inductor $L_f$.}
          Between the AC string and the grid.
          Attenuates PWM harmonics; forces $\ig$ to be approximately
          sinusoidal.
          Crucially, the \emph{same} $\ig$ flows through every cell ---
          there is no alternative current path.

    \item \textbf{Grid.}
          Modelled as $v_{\mathrm{grid}}(t) = V_{\mathrm{grid}}\sin(\omg t + \phig)$.
          The grid fixes frequency $\omg$ and power-factor angle $\phig$;
          the CHB controls amplitude and phase of $\vg$ to regulate $\ig$.
\end{enumerate}
\end{circuitbox}

% ── §1.3 ─────────────────────────────────────────────────────────────────
\subsection{Loading Power and the Capacitor Equation}
\label{subsec:loading_power}

Because the same current $\ig$ passes through every cell, each cell
\emph{must} process its share of AC power.
Each cell also has its own DC source pushing power in.
The net power that charges or discharges the DC-link capacitor of cell $i$
is its \textbf{loading power}:

\begin{eqnbox}[Definition of Loading Power]
\[
    \PHi{i} \;=\; \frac{1}{T}\int_{0}^{T} v_i(t)\,\ig(t)\,\mathrm{d}t
\]
where $T = 1/f$ is one grid period.
This is the average AC power \emph{delivered by cell $i$ to the grid}
(positive = delivering, negative = absorbing).
\end{eqnbox}

The paper calls this quantity the \textbf{cell loading power} (written as $P_{iH}$).

The DC-link capacitor stores energy $E_i = \tfrac{1}{2}C\Vdci{i}^2$.
Two flows act on it simultaneously: $P_{i,\mathrm{src}}$ in from the DC
source, and $\PHi{i}$ out to the AC string.
Their net difference gives the governing equation:

\begin{eqnbox}[Capacitor Energy Balance]
\[
    C\,\Vdci{i}\,\frac{d\Vdci{i}}{dt}
    \;=\; P_{i,\mathrm{src}} - \PHi{i}
\]
\end{eqnbox}

\begin{physbox}[Three consequences of this equation]
\begin{enumerate}
    \item \textbf{Steady state} ($\dot{V}_{dc,iH} = 0$) requires
          $\PHi{i} = P_{i,\mathrm{src}}$ every grid cycle.
    \item \textbf{Overcharging:} $P_{i,\mathrm{src}} > \PHi{i}$
          $\Rightarrow$ $\Vdci{i}$ rises.
    \item \textbf{Discharging:} $P_{i,\mathrm{src}} < \PHi{i}$
          $\Rightarrow$ $\Vdci{i}$ falls.
\end{enumerate}
This is why the paper focuses entirely on controlling $\PHi{i}$:
it is the only quantity the modulator can set to keep $\Vdci{i}$
at its target.
\end{physbox}

% ── §1.4 ─────────────────────────────────────────────────────────────────
\subsection{The Balancing Problem}
\label{subsec:problem}

\begin{warningbox}[Why DC voltages drift --- and why it matters]
The grid current $\ig$ is \emph{shared} by all cells.
The DC source powers $P_{i,\mathrm{src}}$ are \emph{independent}:
they depend on sunlight, battery state of charge, or deliberate power
routing, and can differ arbitrarily between cells.

If cell~1 receives more source power than cell~2, the only way to
prevent $\Vdci{1}$ from rising and $\Vdci{2}$ from falling is to
\emph{make cell~1 deliver more loading power than cell~2}.
But both cells share the same $\ig$ --- the only freedom is the
per-cell modulation index $\Mi$.

Left uncontrolled, one cell charges to destruction while another
discharges to zero. Balancing is therefore a hard requirement, not
an optional add-on.
\end{warningbox}

This imbalance occurs in every practical CHB application:
\begin{itemize}
    \item \textbf{Photovoltaic strings} --- different panels see
          different shading.
    \item \textbf{Battery packs} --- cells age at different rates and
          reach different states of charge.
    \item \textbf{Power routing systems} --- cells are deliberately
          assigned unequal power to extend lifetime.
\end{itemize}

% ── §1.5 ─────────────────────────────────────────────────────────────────
\subsection{Symbol Table}
\label{subsec:symbols}

\begin{statebox}[Master Symbol Table]
\renewcommand{\arraystretch}{1.3}
\begin{tabular}{p{0.17\linewidth} p{0.42\linewidth} p{0.33\linewidth}}
\toprule
\textbf{Symbol} & \textbf{Meaning} & \textbf{Typical Value / Units} \\
\midrule
$n$                    & Number of H-bridge cells            & 3 (prototype), 4 (analysis) \\
$\Vdci{i}$            & DC-link voltage, cell $i$           & 600\,V (target each) \\
$\Vtot$                & Total DC voltage $\sum_i \Vdci{i}$ & $n \times 600$\,V \\
$\ig$, $\Ig$          & Grid current (inst.\ / amplitude)   & A; same through all cells \\
$\omg$                 & Grid angular frequency              & $2\pi \times 50$\,rad/s \\
$\phig$                & Grid power factor angle             & Fixed by load \\
$L_f$                  & Grid-side filter inductance         & 1\,mH \\
$\Mi$                  & Modulation index, cell $i$          & $0 \leq M_i \leq 1$; nominal 0.78 \\
$\phic$                & Clamping angle (paper notation)     & 0 (no clamping) to $\pi$ (maximum clamping) \\
$\PH$                  & Loading power (average per cycle)   & W \\
$\PHi{i}$             & Loading power, cell $i$             & W; equals $P_{i,\mathrm{src}}$ in steady state \\
$\PHnc$                & Non-clamped loading power           & fn.\ of $\Mi$ only \\
$\PHcl$                & Clamped loading power               & fn.\ of $\Mi$ and $\phic$ \\
$P_{i,\mathrm{src}}$  & DC source power, cell $i$           & W \\
$f_{sw}$               & Switching frequency                 & 10\,kHz \\
$f_{bal}$              & Balancing update frequency          & 3.6\,kHz \\
\bottomrule
\end{tabular}
\end{statebox}

\clearpage

% ============================================================
\section{The Control Architecture: Three Loops}
\label{sec:control}
% ============================================================

\textit{This section shows the complete control picture in one place --- the three
nested loops that govern the CHB --- and then explains the conventional PI
balancing method and why it is slow.}

% ── §2.1 ─────────────────────────────────────────────────────────────────
\subsection{The Three-Layer Control Scheme}
\label{subsec:three_loops}

\figentry{fig:paper_fig2}{DLVBMCHBC_Fig2_page02}%
    {Three-layer control scheme for the grid-connected CHB converter
     (paper Fig.\,2).
     \textbf{Outer loop (DC-link voltage control, slow):} compares the
     average DC-link voltage to its reference and generates the grid
     current reference $\ig^{*}$.
     \textbf{Inner loop (grid current control, fast):} forces the actual
     grid current to track $\ig^{*}$ and produces the total voltage
     reference $\vref$.
     \textbf{Balancing layer (per-cell):} divides $\vref$ among the $n$
     cells by computing individual modulation indices $M_i$ (and clamping
     angles $\phic$ in the proposed method).}{0.9}

\begin{circuitbox}[Three-Layer Walk-through (paper Fig.\,2)]
\begin{enumerate}
    \item \textbf{Outer loop --- DC-link voltage control (slow).}
          Measures the average DC-link voltage
          $\bar{V}_{dc} = \frac{1}{n}\sum_i \Vdci{i}$,
          compares it to the reference $V_{dc}^{*}$, and passes the error
          through a PI controller.
          Output: grid current reference $\Ig^{*}$.
          This loop is slow (bandwidth $\approx f_{bal}$) because it
          must not disturb the inner current loop.

    \item \textbf{Inner loop --- grid current control (fast).}
          Measures the actual grid current $\ig$, compares it to $\Ig^{*}$,
          and passes the error through another PI (or resonant) controller.
          Output: total voltage reference $\vref$.
          This loop is fast (bandwidth $\gg f$) to achieve low current THD.

    \item \textbf{Balancing layer --- per-cell modulation.}
          Takes $\vref$ and the measured individual voltages $\Vdci{i}$,
          and computes a per-cell output reference $v_{iH}^{*}$ for each cell.
          In the conventional method this is done with per-cell PI
          controllers (paper Fig.\,3).
          In the proposed method it is done by the sorting algorithm
          with clamped/non-clamped modulation (Sections~\ref{sec:proposed}
          and~\ref{sec:algorithm}).
\end{enumerate}
\end{circuitbox}

The key constraint linking all three layers is the \emph{coupling constraint}:
all cells share the same $\ig$, so their voltage references must sum to $\vref$:

\begin{eqnbox}[Coupling Constraint]
\[
    \sum_{i=1}^{n} v_{iH}^{*} \;=\; \vref
    \qquad
    \Longleftrightarrow
    \qquad
    \sum_{i=1}^{n} \Vdci{i}\,\Mi \;=\; \vref
\]
Any increase in one cell's $\Mi$ must be compensated by decreases in
the others, or the total voltage reference is violated.
This coupling is what makes balancing non-trivial.
\end{eqnbox}

% ── §2.2 ─────────────────────────────────────────────────────────────────
\subsection{Conventional PI Balancing --- The Method This Paper Replaces}
\label{subsec:conventional_pi}

\figentry{fig:paper_fig3}{DLVBMCHBC_Fig3_page02}%
    {Conventional PI-based DC-link voltage balancing (paper Fig.\,3).
     Each cell $i$ has a dedicated PI controller that compares
     $\Vdci{i}$ to the average voltage $\bar{V}_{dc}$ and adds a
     correction $\Delta v_{iH}$ to the shared voltage reference $\vref$,
     giving a per-cell reference $v_{iH}^{*} = \vref + \Delta v_{iH}$.
     All $n$ correction terms must sum to zero to satisfy the coupling
     constraint.}{0.9}

\begin{circuitbox}[How Conventional PI Balancing Works]
Each cell runs an independent PI loop:
\[
    v_{iH}^{*}(k) \;=\; \vref(k) \;+\; \Delta v_{iH}(k)
\]
where $\Delta v_{iH}$ is proportional-plus-integral of the individual
voltage error $(\Vdci{i} - \bar{V}_{dc})$.

The controller must respect the coupling constraint
$\sum_i \Delta v_{iH} = 0$: if one cell's correction increases,
another's must decrease by the same amount.
\end{circuitbox}

\textbf{Why it is slow.}
The PI bandwidth cannot be set freely.
There is a strict hierarchy of time constants:
\[
    t_{sw} \;\ll\; t_{c} \;\ll\; t_{v} \;\ll\; t_{bal}
\]
where $t_{sw}$ is the switching period, $t_c$ the current-loop bandwidth,
$t_v$ the voltage-loop bandwidth, and $t_{bal}$ the balancing settling time.
The balancing loop must be \emph{slower} than the voltage loop to avoid
destabilising it.
This intrinsic hierarchy means the PI controller can only correct a tiny
fraction of the voltage imbalance each grid cycle.
Result: settling time $\approx 1850$\,ms (92 grid cycles at 50\,Hz).

\begin{warningbox}[The fundamental limitation of PI balancing]
The PI adds a small $\Delta v_{iH}$ each cycle.
The maximum step size is limited by the requirement not to distort the
grid current.
No matter how aggressively the gains are tuned, the settling time is
bounded below by the bandwidth hierarchy.
This is the limitation the paper overcomes --- not by better PI tuning,
but by replacing the architecture entirely.
\end{warningbox}

\clearpage

% ============================================================
\section{The Proposed Method: Modulation-Based Balancing}
\label{sec:proposed}
% ============================================================

\textit{This section rebuilds the proposed method in the same order as the IEEE
paper: first the principle (two complementary modulation signals), then how it is
updated in time, and finally why it increases the range of achievable loading
power.}

% ── §3.1 ─────────────────────────────────────────────────────────────────
\subsection{Principle: Two Complementary Modulation Signals}
\label{subsec:principle}

The inner current controller (Section~\ref{sec:control}) produces a
\emph{sinusoidal} reference signal $v^{*}$.
In the paper, $v^{*}$ is a \textbf{normalised} signal (bounded within the linear
modulation region), and $n\,v^{*}$ represents the total normalised reference of
the full CHB string.
If the sum of the cell outputs follows this reference, the grid current can be
kept sinusoidal.
The key trick in the paper is to let \emph{individual} cells use a discontinuous
signal, as long as the \emph{sum across cells} is still sinusoidal.

The method defines two operation modes for a cell: \textbf{clamped} and
\textbf{non-clamped}.
The clamped modulation signal depends only on the sign of the grid voltage:

\begin{eqnbox}[Clamped and Non-Clamped Signals (paper Eq.\,2--3)]
\[
v_{c}^{*} 
=
\begin{cases}
1 & \text{if } \vg > 0 \\
-1 & \text{if } \vg < 0
\end{cases}
\qquad
v_{nc}^{*} = n\,v^{*} - v_{c}^{*}
\]
\[
v_{c}^{*} + v_{nc}^{*} = n\,v^{*}
\]
\end{eqnbox}

Read this as a construction rule:
the algorithm creates two waveforms whose combined effect preserves the sinusoidal
total reference demanded by the inner current loop.

\figentry{fig:paper_fig4}{DLVBMCHBC_Fig4_page03}%
    {Principle of the proposed method (paper Fig.\,4).
     Measured cell voltages are sorted and compared to the reference.
     Based on the sign of $\vg\ig$, cells are assigned either clamped
     ($v_{c}^{*}$) or non-clamped ($v_{nc}^{*}$) modulation.
     The key constraint is that the sum of all cell modulation signals remains
     sinusoidal (paper Eq.\,3).}{0.9}

\begin{physbox}[Why this helps balancing]
In conventional PI balancing, each cell stays sinusoidal and can only change its
loading power by changing its sinusoidal amplitude (bounded by $|M|\le 1$).
The proposed method adds a new lever: \emph{waveform shape}.
By clamping some cells to $\pm 1$ for part of the cycle, the method changes the
fundamental component of the cell voltage, and therefore changes the average
loading power it can process.
\end{physbox}

% ── §3.2 ─────────────────────────────────────────────────────────────────
\subsection{When the Algorithm Updates (and Why It Can Be Fast)}
\label{subsec:implementation_overview}

The balancing decision is recomputed periodically with update period $t_{bal}$.
The paper chooses $t_{bal}$ such that it is a divisor of the grid period:
\[
t_f = a\,t_{bal}, \quad a \in \mathbb{Z}
\]
so the assignment can change multiple times within one grid cycle.

\begin{statebox}[The speed advantage in one sentence]
In the conventional PI method, the balancing dynamics are constrained by the
outer voltage loop bandwidth (the hierarchy $t_{sw} \ll t_c \ll t_v \ll t_{bal}$).
In the proposed method, the balancing update $t_{bal}$ is not tied to $t_v$ and
$t_c$ in the same way, so it can be made much faster.
\end{statebox}

This section only establishes \emph{what} is updated and \emph{when}.
The exact sorting and assignment rules (including the role of the sign of
$\vg\ig$ and the multi-cell pairing cases) are the topic of
Section~\ref{sec:algorithm}.

% ── §3.3 ─────────────────────────────────────────────────────────────────
\subsection{Why Clamping Increases the Loading-Power Range}
\label{subsec:capability}

The average loading power of a cell depends on how its voltage waveform lines up
with the (approximately sinusoidal) grid current over one grid period.
The important point is that only the \textbf{fundamental component} of the cell
voltage contributes to the cycle-average power exchange; higher harmonics average
to zero over a full period.

To quantify this, the paper introduces a \textbf{clamping angle} $\phic$ and varies
it from $0$ to $\pi$ (0 to 180 degrees) (paper Fig.\,7).
In this tutorial we will place and walk through paper Fig.\,7 and paper Fig.\,8 in
Section~\ref{sec:algorithm} so the figures appear in the same sequence as the
paper's narrative.

The paper computes the fundamental magnitude using Fourier series.
For a periodic signal $f(\theta)$, the fundamental coefficients are:

\begin{eqnbox}[Fundamental From Fourier Coefficients (paper Eq.\,4--5)]
\[
a_1 = \frac{1}{\pi}\int_{0}^{2\pi} f(\theta)\cos\theta\,d\theta,
\qquad
b_1 = \frac{1}{\pi}\int_{0}^{2\pi} f(\theta)\sin\theta\,d\theta
\]
\[
F = \sqrt{a_1^2 + b_1^2}
\]
\end{eqnbox}

Using the piecewise clamped waveform (paper Eq.\,6) with nominal modulation index
$M$ (paper notation):

\begin{eqnbox}[Piecewise Clamped Waveform (paper Eq.\,6)]
\[
f_c(\theta)=
\begin{cases}
1 & \theta\in[0,\phic]\\
M\sin\theta & \theta\in[\phic,\pi] \text{ and } \theta\in[\pi+\phic,2\pi]\\
-1 & \theta\in[\pi,\pi+\phic]
\end{cases}
\]
\end{eqnbox}

\begin{physbox}[Sine vs. cosine in paper Eq.\,6]
In the IEEE paper, the middle segment is written as $M\cos\theta$.
Using $M\sin\theta$ instead is just a phase shift in the definition of $\theta$.
The Fourier coefficients $a_1$ and $b_1$ change, but the fundamental magnitude
$F_c(\phic)=\sqrt{a_1^2+b_1^2}$ (and therefore the loading-power capability)
is unchanged.
\end{physbox}

we can compute the fundamental explicitly.

\begin{circuitbox}[Step-by-step: deriving paper Eq.\,7]
We compute the first harmonic coefficients of $f_c(\theta)$:
\[
a_1 = \frac{1}{\pi}\int_{0}^{2\pi} f_c(\theta)\cos\theta\,d\theta,
\qquad
b_1 = \frac{1}{\pi}\int_{0}^{2\pi} f_c(\theta)\sin\theta\,d\theta
\]

\textbf{Step 1: split the integral into the four regions.}
\[
\int_{0}^{2\pi} (\cdot)\,d\theta
=
\int_{0}^{\phic} (\cdot)\,d\theta
+\int_{\phic}^{\pi} (\cdot)\,d\theta
+\int_{\pi}^{\pi+\phic} (\cdot)\,d\theta
+\int_{\pi+\phic}^{2\pi} (\cdot)\,d\theta
\]

\textbf{Step 2: compute $a_1$.}
\[
\pi a_1
=
\int_{0}^{\phic} 1\cdot\cos\theta\,d\theta
+\int_{\phic}^{\pi} M\sin\theta\cos\theta\,d\theta
+\int_{\pi}^{\pi+\phic} (-1)\cdot\cos\theta\,d\theta
+\int_{\pi+\phic}^{2\pi} M\sin\theta\cos\theta\,d\theta
\]
Use $\int \cos\theta\,d\theta = \sin\theta$ and
$\int \sin\theta\cos\theta\,d\theta = \tfrac{1}{2}\sin^2\theta$:
\[
\pi a_1
= \sin\phic + \sin\phic + M\left(\tfrac{1}{2}\sin^2\pi - \tfrac{1}{2}\sin^2\phic\right)
+ M\left(\tfrac{1}{2}\sin^2 2\pi - \tfrac{1}{2}\sin^2(\pi+\phic)\right)
\]
Since $\sin\pi = \sin 2\pi = 0$ and $\sin(\pi+\phic) = -\sin\phic$:
\[
\pi a_1 = 2\sin\phic - M\sin^2\phic
= \sin\phic\,\bigl(2 - M\sin\phic\bigr)
\]

\textbf{Step 3: compute $b_1$.}
\[
\pi b_1
=
\int_{0}^{\phic} 1\cdot\sin\theta\,d\theta
+\int_{\phic}^{\pi} M\sin^2\theta\,d\theta
+\int_{\pi}^{\pi+\phic} (-1)\cdot\sin\theta\,d\theta
+\int_{\pi+\phic}^{2\pi} M\sin^2\theta\,d\theta
\]
Use $\int \sin\theta\,d\theta = -\cos\theta$ and
$\int \sin^2\theta\,d\theta = \tfrac{\theta}{2} - \tfrac{\sin 2\theta}{4}$:
\[
\pi b_1
= (1-\cos\phic) + (1-\cos\phic)
+ M\left[\left(\tfrac{\pi-\phic}{2} + \tfrac{\sin 2\phic}{4}\right)
+\left(\tfrac{\pi-\phic}{2} + \tfrac{\sin 2\phic}{4}\right)\right]
\]
So:
\[
\pi b_1 = 2 - 2\cos\phic + M(\pi-\phic) + \tfrac{1}{2}M\sin(2\phic)
\]

\textbf{Step 4: fundamental magnitude.}
The fundamental magnitude of $f_c$ is
$F_c(\phic) = \sqrt{a_1^2 + b_1^2}$, which gives paper Eq.\,7.
\end{circuitbox}

Putting $a_1$ and $b_1$ into $F_c = \sqrt{a_1^2+b_1^2}$ gives the paper's closed form
for the clamped signal fundamental, and the complementary non-clamped fundamental
(paper Eq.\,7):

\begin{eqnbox}[Fundamental Magnitudes (paper Eq.\,7)]
\[
F_c(\phic) = \frac{1}{\pi}
\sqrt{\left(0.5M\sin(2\phic) - M\phic - 2\cos\phic + \pi M + 2\right)^2
+ \left(\sin\phic\,(2 - M\sin\phic)\right)^2}
\]
\[
F_{nc}(\phic) = 2M - F_c(\phic)
\]
\end{eqnbox}

\begin{statebox}[Numbers to remember from paper Fig.\,8]
At $M=0.7$ and $\phic=\pi$ (180 degrees), the paper reports approximately
1.82 p.u. loading power in clamped mode and 0.18 p.u. in non-clamped mode.
That large spread is the ``extra room'' the algorithm uses to move energy between
cells quickly.
\end{statebox}

\clearpage

% ============================================================
\section{The Sorting Algorithm}
\label{sec:algorithm}
% ============================================================

\textit{This section turns paper Section III-B into a step-by-step recipe.
We will answer two questions:
(1) how the controller decides which cells get clamped vs non-clamped signals,
and (2) how that decision scales from 2 cells to $n$ cells.
The key paper figures here are Fig.\,5--Fig.\,8.}

% ── §4.1 ─────────────────────────────────────────────────────────────────
\subsection{Sorting Inputs, Pairing Rule, and the Sign of $\vg\ig$}
\label{subsec:sorting}

Every balancing update (every $t_{bal}$), the controller measures all cell voltages
$\Vdci{i}$ and compares them to the per-cell reference $V_{dc,H}^{*}$.
Then it sorts the cells from highest voltage to lowest voltage.

The paper uses the sign of instantaneous grid power $\vg\ig$ to decide whether
clamping should \emph{charge} a low-voltage cell or \emph{discharge} a high-voltage
cell.

\begin{statebox}[Sign Rule (paper Section III-A/B)]
\textbf{Rectifier mode} ($\vg\ig < 0$): grid power flows \emph{into} the CHB and can
charge the capacitors.
\begin{itemize}
    \item If a cell is \textbf{undercharged} ($\Vdci{i} < V_{dc,H}^{*}$), assign it
          \textbf{clamped} mode ($v_c^{*}$) so it charges faster.
    \item If a cell is \textbf{overcharged} ($\Vdci{i} > V_{dc,H}^{*}$), assign it
          \textbf{non-clamped} mode ($v_{nc}^{*}$) so it charges slower (or
          effectively discharges relative to the others).
\end{itemize}

\textbf{Inverter mode} ($\vg\ig > 0$): power flows \emph{from} the CHB to the grid.
The assignments swap: overcharged cells are clamped to discharge faster, and
undercharged cells are non-clamped to discharge slower.
\end{statebox}

\textbf{The pairing rule.}
The clamped and non-clamped signals are defined to be complementary
(Section~\ref{sec:proposed}, paper Eq.\,3).
To keep the total reference sinusoidal, the paper assigns cells in \emph{pairs}:
each clamped cell is paired with a non-clamped cell.
Any remaining unpaired cell(s) use the original sinusoidal reference $v^{*}$.

% ── §4.2 ─────────────────────────────────────────────────────────────────
\subsection{Two-Cell Case Study (paper Fig.\,5)}
\label{subsec:two_cell}

Paper Fig.\,5 illustrates the idea with $n=2$ cells and an intentionally slow
update period $t_{bal}=5$\,ms (so the regions are easy to see).
The figure assumes $\vg\ig<0$ (rectifier mode).

\figentry{fig:paper_fig5}{DLVBMCHBC_Fig5_page03}%
    {Two-cell case study with $t_{bal}=5$\,ms (paper Fig.\,5).
     The controller updates the mode assignment every $t_{bal}$, so one grid
     cycle is split into four regions (I--IV).
     For $\vg\ig<0$, the lower-voltage cell is clamped to charge faster, and the
     higher-voltage cell is non-clamped to charge slower.}{0.9}

The four regions correspond to four possible orderings around the reference
$V_{dc,H}^{*}$:

\begin{circuitbox}[Region-by-Region Logic for $n=2$ (rectifier mode)]
\begin{enumerate}
    \item \textbf{Region I:} $\Vdci{2} > V_{dc,H}^{*} > \Vdci{1}$.
          Cell~1 is undercharged $\Rightarrow$ clamped; cell~2 is overcharged
          $\Rightarrow$ non-clamped.

    \item \textbf{Region II:} $\Vdci{1} > V_{dc,H}^{*} > \Vdci{2}$.
          The ordering flips, so the assignment flips: cell~2 clamped, cell~1
          non-clamped.

    \item \textbf{Region III:} $\Vdci{1} > \Vdci{2} > V_{dc,H}^{*}$.
          Both cells are above the reference.
          The paper's rule is to clamp the \emph{lower} of the two voltages and
          non-clamp the higher one, so the voltages move toward each other.

    \item \textbf{Region IV:} $V_{dc,H}^{*} > \Vdci{2} > \Vdci{1}$.
          Both cells are below the reference.
          Clamp the lowest-voltage cell (cell~1) and non-clamp the higher one
          (cell~2).
\end{enumerate}
\end{circuitbox}

Even in this simple case you can see the algorithm's character:
it does not apply a tiny correction like a PI loop.
It \emph{switches between two waveform shapes} to quickly change the loading power
each cell processes.

% ── §4.3 ─────────────────────────────────────────────────────────────────
\subsection{Four-Cell Case: The Five Sorting Cases (paper Fig.\,6)}
\label{subsec:four_cell}

For more than two cells, the main new question is:
\emph{which cells should be paired?}
Paper Fig.\,6 lists the five distinct cases that can occur for $n=4$.
The same logic extends to any $n$.

\figentry{fig:paper_fig6}{DLVBMCHBC_Fig6_page04}%
    {Five cases for four cells (paper Fig.\,6).
     Without bracket: $\vg\ig<0$ (rectifier mode).
     With bracket: $\vg\ig>0$ (inverter mode).
     Cells are sorted by voltage; the controller forms clamped/non-clamped pairs
     so the total modulation remains sinusoidal.}{0.95}

\begin{statebox}[How to read paper Fig.\,6]
\textbf{Step 1: sort} the cell voltages.
\textbf{Step 2: locate} where $V_{dc,H}^{*}$ lies relative to the sorted list.
\textbf{Step 3: pair} high-voltage cells with low-voltage cells.
In rectifier mode ($\vg\ig<0$), the low-voltage cell in each pair is clamped and
the high-voltage cell is non-clamped; in inverter mode the assignment reverses.
Any leftover unpaired cell(s) remain sinusoidal.
\end{statebox}

The key point is that Fig.\,6 is not describing ``five numbers of clamped cells''.
It is describing five different \emph{sorting outcomes} (where the reference sits
among the sorted voltages) and the resulting pairing pattern.

% ── §4.4 ─────────────────────────────────────────────────────────────────
\subsection{Clamping Angle $\phic$ (paper Fig.\,7)}
\label{subsec:clamp_angle}

Once a cell is assigned clamped mode, the next knob is \emph{how much} of the cycle
it stays clamped.
The paper parameterises this with the clamping angle $\phic \in [0,\pi]$.

\figentry{fig:paper_fig7}{DLVBMCHBC_Fig7_page04}%
    {Clamped modulation signal with varied clamping angle $\phic$ (paper Fig.\,7).
     Larger $\phic$ widens the flat region, increasing the fundamental magnitude
     of the clamped waveform and therefore increasing the cell's achievable
     loading power.}{0.85}

At $\phic=0$ there is no clamping.
At $\phic=\pi$ the waveform is maximally clamped.
Section~\ref{sec:proposed} derived how the fundamental magnitude $F_c(\phic)$
varies with $\phic$.

% ── §4.5 ─────────────────────────────────────────────────────────────────
\subsection{What the Capability Surface Means (paper Fig.\,8)}
\label{subsec:surface}

Paper Fig.\,8 plots the resulting loading power capability versus nominal
modulation index $M$ and clamping angle $\phic$.

\figentry{fig:paper_fig8}{DLVBMCHBC_Fig8_page04}%
    {Loading power as a function of nominal modulation index $M$ and clamping
     angle $\phic$ (paper Fig.\,8).
     (a) clamped mode: achievable loading power increases with $\phic$.
     (b) non-clamped mode: achievable loading power decreases accordingly.
     This spread is the range the sorting algorithm exploits to balance cell
     voltages quickly.}{0.95}

\begin{physbox}[Why this figure matters]
In PI balancing, you can only move loading power by changing the sinusoidal
amplitude, and you hit the hard limit $|M|\le 1$.
In the proposed method, clamping changes the waveform's fundamental magnitude.
That creates a much wider range of possible loading powers, so energy can be moved
between cells quickly without waiting many grid cycles.
\end{physbox}

\clearpage

% ============================================================
\section{Performance: Simulation and Experiment}
\label{sec:results}
% ============================================================

\textit{This section validates the proposed method through comprehensive
simulation studies and experimental results. The simulations demonstrate
the method's superior balancing capability and dynamic response, while
the experiments confirm its practical feasibility on a real CHB prototype.}

% ── §5.1 ─────────────────────────────────────────────────────────────────
\subsection{Simulation Validation}
\label{subsec:simulation}

The feasibility of the proposed method is first validated through simulation
using a six-cell CHB converter with the following specifications:

\begin{statebox}[Simulation Parameters]
\begin{tabular}{p{0.4\linewidth} p{0.5\linewidth}}
\toprule
\textbf{Parameter} & \textbf{Value} \\
\midrule
Grid voltage amplitude ($V_g$) & 2\,kV \\
Grid current amplitude ($I_g$) & 60\,A \\
Filter inductance ($L_f$) & 1\,mH \\
Cell DC voltage ($V_{dc,i}$) & 600\,V (nominal) \\
Nominal modulation index ($M$) & 0.78 \\
Switching frequency ($f_{sw}$) & 10\,kHz \\
Balancing update frequency ($f_{bal}$) & 3.6\,kHz \\
IGBT device & IXYB82N120C3H1 \\
\bottomrule
\end{tabular}
\end{statebox}

The balancing update frequency of 3.6\,kHz corresponds to 72 updates per
grid cycle (50\,Hz), enabling very fast response compared to conventional methods.

\figentry{fig:paper_fig9}{DLVBMCHBC_Fig9_page05}%
    {Comparison of maximum loading power capability with conventional method
     (paper Fig.\,9).
     The proposed method achieves 27\% higher maximum loading power across
     different nominal modulation indices, extending the operating range for
     applications with severe power imbalances.}{0.8}

\figentry{fig:paper_fig10}{DLVBMCHBC_Fig10_page06}%
    {Control performance comparison under power imbalance conditions
     (paper Fig.\,10).
     (a) Conventional PI method shows slow response and ripple voltage
     amplitude differences between cells.
     (b) Proposed method with unity power factor achieves balanced ripple
     amplitudes and fast settling.
     (c) Proposed method maintains performance at 0.8 power factor.
     Loading power ratio changes from 1:1 to 1.25:0.75 at $t=0.5$\,s.}{0.95}

\figentry{fig:paper_fig11}{DLVBMCHBC_Fig11_page06}%
    {Maximum control performance comparison (paper Fig.\,11).
     (a) Conventional method fails when loading power ratio exceeds
     capability limits (modulation index saturates at $\pm 1$).
     (b) Proposed method successfully balances voltages with extreme
     imbalance (1.62:0.38 ratio), utilizing square-wave modulation.
     Power factor = -1, $M=0.78$.}{0.95}

\begin{circuitbox}[Key Simulation Insights]
\begin{enumerate}
    \item \textbf{Maximum capability:} The proposed method handles 27\% higher
          loading power imbalance than conventional PI control (Fig.\,9).
    \item \textbf{Dynamic response:} Settling time improves from 0.3\,s to
          milliseconds (Fig.\,10), independent of power factor (Fig.\,10c).
    \item \textbf{Voltage ripple:} All cells maintain identical ripple amplitudes
          due to fast balancing updates, unlike the conventional method.
    \item \textbf{Extreme imbalance:} At 1.62:0.38 power ratio, the method
          uses square-wave modulation (Fig.\,11b) to achieve the theoretical
          maximum capability.
\end{enumerate}
\end{circuitbox}

% ── §5.2 ─────────────────────────────────────────────────────────────────
\subsection{Experimental Validation}
\label{subsec:experiment}

The proposed method is experimentally validated on a three-cell CHB prototype
connected to the real grid (230\,V$_{\mathrm{rms}}$). The setup uses lower-voltage
components for safety but maintains the same control principles.

\begin{statebox}[Experimental Setup Parameters]
\begin{tabular}{p{0.4\linewidth} p{0.5\linewidth}}
\toprule
\textbf{Parameter} & \textbf{Value} \\
\midrule
Grid voltage & 230\,V$_{\mathrm{rms}}$ (real grid) \\
Cell DC voltage (nominal) & 180\,V \\
Modulation index ($M$) & 0.6 \\
Switching frequency ($f_{sw}$) & 10\,kHz \\
Operation mode & Rectifier ($\vg\ig < 0$) \\
Load emulation & Resistor banks with unbalancing resistors \\
\bottomrule
\end{tabular}
\end{statebox}

\figentry{fig:paper_fig12}{DLVBMCHBC_Fig12_page06}%
    {Developed three-cell CHB prototype (paper Fig.\,12).
     The converter connects directly to the real grid with resistor banks
     on DC links to emulate unbalanced loading powers.
     Unbalancing resistors create the power imbalance conditions.}{0.75}

\figentry{fig:paper_fig13}{DLVBMCHBC_Fig13_page07}%
    {Steady-state control performance comparison (paper Fig.\,13).
     Top row: conventional PI method; bottom row: proposed method.
     (a) Balanced condition (P = 1:1:1), (b) moderate imbalance (1.2:1:0.8),
     (c) severe imbalance (1.4:1:0.6).
     The proposed method maintains voltage regulation even at extreme
     imbalances where conventional method fails.}{0.95}

\figentry{fig:paper_fig14}{DLVBMCHBC_Fig14_page07}%
    {Dynamic response comparison (paper Fig.\,14).
     Loading power ratio = 6:4:2 (3:2:1 normalized).
     (a) Conventional PI method: 1.85\,s settling time.
     (b) Proposed method: 140\,ms settling time (13$\times$ faster).
     The improvement comes from the modulation-based approach, not
     faster PI tuning.}{0.95}

\begin{warningbox}[Experimental Results Summary]
\begin{enumerate}
    \item \textbf{Steady-state balancing:} Successfully regulates DC voltages
          under extreme imbalances (1.4:1:0.6) where conventional method fails.
    \item \textbf{Dynamic performance:} 13$\times$ faster settling time
          (1.85\,s vs 140\,ms) due to modulation-based balancing.
    \item \textbf{Real-world validation:} Results on actual grid-connected
          hardware confirm simulation predictions and theoretical analysis.
    \item \textbf{Scalability:} The three-cell prototype validates the method
          for practical implementation; principles extend to higher cell counts.
\end{enumerate}
\end{warningbox}

\clearpage
