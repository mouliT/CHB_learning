% ============================================================
%  DC-Link Voltage Balancing Modulation for Cascaded H-Bridge Converters
%  Paper 3 Reference Notes
%  Source: Ko, Tcai, Liserre, IEEE Access 2021
%  Figures stored in: figures/
%  Naming convention: DLVBMCHBC_Fig[N]_page[XX].png  (caption-extracted at 200 DPI)
% ============================================================
\documentclass[12pt, a4paper]{article}

% ---- core packages ----
\usepackage[utf8]{inputenc}
\usepackage[T1]{fontenc}
\usepackage{amsmath, amssymb, amsthm}
\usepackage{bm}
\usepackage{siunitx}

% ---- figures ----
\usepackage{graphicx}
\graphicspath{{figures/}}
\usepackage{float}
\usepackage{caption}
\usepackage{subcaption}

% ---- layout ----
\usepackage[a4paper, top=2.5cm, bottom=2.5cm, left=3cm, right=2.8cm]{geometry}
\usepackage{parskip}
\setlength{\parskip}{6pt}
\setlength{\parindent}{0pt}

% ---- hyperlinks ----
\usepackage[colorlinks=true, linkcolor=blue!60!black, citecolor=blue!50!black]{hyperref}
\usepackage{bookmark}
\makeatletter
\pdfstringdefDisableCommands{%
    \def\vspace#1{}%
    \def\rule#1#2{}%
    \def\color#1{}%
    \let\leavevmode@ifvmode\relax%
    \def\,{ }%
}
\makeatother

% ---- styled boxes ----
\usepackage{tcolorbox}
\tcbuselibrary{skins, breakable}

% ---- tables ----
\usepackage{booktabs}
\usepackage{array}
\usepackage{longtable}

% ---- section formatting ----
\usepackage{titlesec}
\usepackage{xcolor}
\definecolor{chbblue}{RGB}{10, 55, 120}
\definecolor{chborange}{RGB}{190, 70, 5}
\definecolor{chbgreen}{RGB}{8, 95, 38}
\definecolor{chbred}{RGB}{170, 25, 25}
\definecolor{lightblue}{RGB}{216, 232, 252}
\definecolor{lightorange}{RGB}{255, 244, 220}
\definecolor{lightgreen}{RGB}{216, 247, 228}
\definecolor{lightred}{RGB}{255, 228, 228}
\definecolor{lightgray}{RGB}{245, 245, 245}

\titleformat{\section}{\large\bfseries\color{chbblue}}{{\thesection.}}{0.5em}{}
            [\vspace{-4pt}\rule{\textwidth}{0.5pt}]
\titleformat{\subsection}{\normalsize\bfseries\color{chbblue!80!black}}{{\thesubsection}}{0.5em}{}

% ---- tcolorbox styles ----
\newtcolorbox{circuitbox}[1][Circuit Description]{
    colback=lightblue, colframe=chbblue, coltitle=white,
    fonttitle=\bfseries\small, title={#1},
    breakable, enhanced, left=4pt, right=4pt, top=4pt, bottom=4pt}

\newtcolorbox{statebox}[1][State Variables \& Parameters]{
    colback=lightorange, colframe=chborange, coltitle=white,
    fonttitle=\bfseries\small, title={#1},
    breakable, enhanced, left=4pt, right=4pt, top=4pt, bottom=4pt}

\newtcolorbox{eqnbox}[1][Governing Equations]{
    colback=lightgreen, colframe=chbgreen, coltitle=white,
    fonttitle=\bfseries\small, title={#1},
    breakable, enhanced, left=4pt, right=4pt, top=4pt, bottom=4pt}

\newtcolorbox{physbox}[1][Physical Interpretation]{
    colback=lightgray, colframe=gray!60, coltitle=white,
    fonttitle=\bfseries\small, title={#1},
    breakable, enhanced, left=4pt, right=4pt, top=4pt, bottom=4pt}

\newtcolorbox{warningbox}[1][Watch Out]{
    colback=lightred, colframe=chbred, coltitle=white,
    fonttitle=\bfseries\small, title={#1},
    breakable, enhanced, left=4pt, right=4pt, top=4pt, bottom=4pt}

% ---- custom commands ----
\newcommand{\vg}{v_{g}}
\newcommand{\Vg}{V_{g}}
\newcommand{\ig}{i_{g}}
\newcommand{\Ig}{I_{g}}
\newcommand{\omg}{\omega}
\newcommand{\phig}{\phi_{g}}
\newcommand{\Vdc}{V_{dc}}
\newcommand{\Vdci}[1]{V_{dc,#1\mathrm{H}}}
\newcommand{\Vtot}{V_{dc,\mathrm{tot}}}
\newcommand{\Mi}{M_{i}}
\newcommand{\phic}{\varphi}
\newcommand{\vref}{v_{\mathrm{ref}}}
\newcommand{\PH}{P_{H}}
\newcommand{\PHi}[1]{P_{#1\mathrm{H}}}
\newcommand{\PHnc}{P_{H}^{\mathrm{nc}}}
\newcommand{\PHcl}{P_{H}^{\mathrm{cl}}}
\newcommand{\figref}[1]{Figure~\ref{#1}}

% ---- figure entry macro ----
\newcommand{\figentry}[4]{%
    \begin{figure}[H]
        \centering
        \includegraphics[width=#4\textwidth]{#2}
        \caption{#3}
        \label{#1}
    \end{figure}%
}

% ============================================================
\title{\textbf{DC-Link Voltage Balancing Modulation}\\[4pt]
       \textbf{for Cascaded H-Bridge Converters}\\[6pt]
       \large A Self-Contained Tutorial\\[4pt]
       \normalsize Ko, Tcai, Liserre --- IEEE Access 2021}
\date{\today}
% ============================================================

\begin{document}
\maketitle
\tableofcontents
\clearpage

% ============================================================
\section{The CHB Converter: Circuit, Cascading, and the Balancing Problem}
\label{sec:circuit}
% ============================================================

\textit{This section builds the CHB from a single cell upward, shows why cascading
gives a better waveform, and identifies the core balancing problem that the paper
solves.}

The paper studies a
\textbf{grid-connected cascaded H-bridge (CHB)} converter where each cell has an
\textbf{isolated DC-link capacitor} and its own DC source.
Because different sources can deliver different power, the cell voltages naturally
drift apart unless the controller actively redistributes how much \emph{AC-side power}
each cell processes.

% ── §1.1 ─────────────────────────────────────────────────────────────────
\subsection{From One Cell to Many: How Cascading Works}
\label{subsec:cascading}

An H-bridge cell has two ports.
On the \textbf{DC side}, an isolated capacitor at voltage $\Vdci{i}$ is fed
by a private source (a PV string, battery, or rectifier) delivering power
$P_{i,\mathrm{src}}$.
On the \textbf{AC side}, four transistors chop that DC voltage at carrier
frequency $f_c \gg f$.
Averaged over one switching period, the fundamental component of the AC
output is:
\[
    \bar{v}_i(t) = \Vdci{i}\,\Mi\,\sin(\omg t)
\]
where $\Mi \in [0,1]$ is the modulation index --- the single knob the
controller turns to set how much power this cell contributes.

In reality the instantaneous $v_i(t)$ is a high-frequency PWM waveform; in this
tutorial we use the fundamental component because it is what transfers power to
the grid at frequency $f$.

Cascading means connecting the AC output terminals of $n$ cells in
\emph{series}.
The same grid current $\ig$ is forced through every cell.
The total voltage seen by the grid is:
\[
    \vg(t) = v_1(t) + v_2(t) + \cdots + v_n(t)
\]

\figentry{fig:staircase_buildup}{CHB_staircase_buildup}%
    {How three individual H-bridge cell outputs combine to form a
     seven-level multilevel waveform ($n = 3$, $f_c = 12f$, $M = 0.95$).
     \textbf{Rows 1--3:} each cell's unipolar PWM output (solid colour)
     with its phase-shifted triangular carrier (grey) and the shared
     sinusoidal reference (dashed).
     \textbf{Row 4 (Sum):} the series sum $\vg = v_1 + v_2 + v_3$
     steps through seven discrete levels and closely tracks the ideal
     sinusoid $3\Vdc M\sin(\omg t)$ (red dashed).
     The orange shading is the residual instantaneous error; phase-shifted
     carriers ensure no two cells switch simultaneously.}{1.0}

\figentry{fig:staircase_summation}{CHB_staircase_summation}%
    {Effect of increasing the number of cells $n = 1, 2, 3, 4$ on output
     waveform quality ($f_c = 6f$, $M = 0.95$).
     Each row shows the total output $\vg$ on a normalised axis
     $(\vg / n\Vdc)$.
     With $n = 1$ the output has only 3 levels and large harmonic error
     (orange shading).
     Each additional cell doubles the number of levels
     (3 $\to$ 5 $\to$ 7 $\to$ 9), shrinking the error region visibly.
     This is why CHB converters dominate high-power applications:
     more cells means lower harmonic distortion and a smaller filter $L_f$
     with no increase in individual transistor voltage rating.}{1.0}

% ── §1.2 ─────────────────────────────────────────────────────────────────
\subsection{Full Circuit Topology}
\label{subsec:topology}

\figentry{fig:paper_fig1}{DLVBMCHBC_Fig1_page01}%
    {Grid-connected CHB converter with unbalanced loading powers
     $\PHi{1} \neq \PHi{2} \neq \PHi{3}$ (paper Fig.\,1).
     Each H-bridge cell has its own isolated DC-link capacitor fed by an
     independent DC source.
     The AC output terminals of all $n$ cells are connected in series;
     the series string drives the grid through filter inductance $L_f$.}{0.75}

\begin{circuitbox}[Component Walk-through (paper Fig.\,1)]
\begin{enumerate}
    \item \textbf{DC sources (right side of each cell).}
          Each cell $i$ has a private DC source delivering $P_{i,\mathrm{src}}$
          to its isolated capacitor at $\Vdci{i}$.
          The dashed isolation boundary is not decorative:
          there is \emph{no} electrical connection between DC sides of
          different cells.

    \item \textbf{H-bridge switches (centre of each cell).}
          Four transistors per cell chop the DC voltage at carrier
          frequency $f_c$.
          The average AC output is
          $\bar{v}_i = \Vdci{i}\,\Mi\,\sin(\omg t)$.

    \item \textbf{AC series string (left column).}
          The top terminal of cell~$i$ connects to the bottom terminal
          of cell~$(i{+}1)$.
          Total output: $\vg = v_1 + \cdots + v_n$.

    \item \textbf{Filter inductor $L_f$.}
          Between the AC string and the grid.
          Attenuates PWM harmonics; forces $\ig$ to be approximately
          sinusoidal.
          Crucially, the \emph{same} $\ig$ flows through every cell ---
          there is no alternative current path.

    \item \textbf{Grid.}
          Modelled as $v_{\mathrm{grid}}(t) = V_{\mathrm{grid}}\sin(\omg t + \phig)$.
          The grid fixes frequency $\omg$ and power-factor angle $\phig$;
          the CHB controls amplitude and phase of $\vg$ to regulate $\ig$.
\end{enumerate}
\end{circuitbox}

% ── §1.3 ─────────────────────────────────────────────────────────────────
\subsection{Loading Power and the Capacitor Equation}
\label{subsec:loading_power}

Because the same current $\ig$ passes through every cell, each cell
\emph{must} process its share of AC power.
Each cell also has its own DC source pushing power in.
The net power that charges or discharges the DC-link capacitor of cell $i$
is its \textbf{loading power}:

\begin{eqnbox}[Definition of Loading Power]
\[
    \PHi{i} \;=\; \frac{1}{T}\int_{0}^{T} v_i(t)\,\ig(t)\,\mathrm{d}t
\]
where $T = 1/f$ is one grid period.
This is the average AC power \emph{delivered by cell $i$ to the grid}
(positive = delivering, negative = absorbing).
\end{eqnbox}

The paper calls this quantity the \textbf{cell loading power} (written as $P_{iH}$).

The DC-link capacitor stores energy $E_i = \tfrac{1}{2}C\Vdci{i}^2$.
Two flows act on it simultaneously: $P_{i,\mathrm{src}}$ in from the DC
source, and $\PHi{i}$ out to the AC string.
Their net difference gives the governing equation:

\begin{eqnbox}[Capacitor Energy Balance]
\[
    C\,\Vdci{i}\,\frac{d\Vdci{i}}{dt}
    \;=\; P_{i,\mathrm{src}} - \PHi{i}
\]
\end{eqnbox}

\begin{physbox}[Three consequences of this equation]
\begin{enumerate}
    \item \textbf{Steady state} ($\dot{V}_{dc,iH} = 0$) requires
          $\PHi{i} = P_{i,\mathrm{src}}$ every grid cycle.
    \item \textbf{Overcharging:} $P_{i,\mathrm{src}} > \PHi{i}$
          $\Rightarrow$ $\Vdci{i}$ rises.
    \item \textbf{Discharging:} $P_{i,\mathrm{src}} < \PHi{i}$
          $\Rightarrow$ $\Vdci{i}$ falls.
\end{enumerate}
This is why the paper focuses entirely on controlling $\PHi{i}$:
it is the only quantity the modulator can set to keep $\Vdci{i}$
at its target.
\end{physbox}

% ── §1.4 ─────────────────────────────────────────────────────────────────
\subsection{The Balancing Problem}
\label{subsec:problem}

\begin{warningbox}[Why DC voltages drift --- and why it matters]
The grid current $\ig$ is \emph{shared} by all cells.
The DC source powers $P_{i,\mathrm{src}}$ are \emph{independent}:
they depend on sunlight, battery state of charge, or deliberate power
routing, and can differ arbitrarily between cells.

If cell~1 receives more source power than cell~2, the only way to
prevent $\Vdci{1}$ from rising and $\Vdci{2}$ from falling is to
\emph{make cell~1 deliver more loading power than cell~2}.
But both cells share the same $\ig$ --- the only freedom is the
per-cell modulation index $\Mi$.

Left uncontrolled, one cell charges to destruction while another
discharges to zero. Balancing is therefore a hard requirement, not
an optional add-on.
\end{warningbox}

This imbalance occurs in every practical CHB application:
\begin{itemize}
    \item \textbf{Photovoltaic strings} --- different panels see
          different shading.
    \item \textbf{Battery packs} --- cells age at different rates and
          reach different states of charge.
    \item \textbf{Power routing systems} --- cells are deliberately
          assigned unequal power to extend lifetime.
\end{itemize}

% ── §1.5 ─────────────────────────────────────────────────────────────────
\subsection{Symbol Table}
\label{subsec:symbols}

\begin{statebox}[Master Symbol Table]
\renewcommand{\arraystretch}{1.3}
\begin{tabular}{p{0.17\linewidth} p{0.42\linewidth} p{0.33\linewidth}}
\toprule
\textbf{Symbol} & \textbf{Meaning} & \textbf{Typical Value / Units} \\
\midrule
$n$                    & Number of H-bridge cells            & 3 (prototype), 4 (analysis) \\
$\Vdci{i}$            & DC-link voltage, cell $i$           & 600\,V (target each) \\
$\Vtot$                & Total DC voltage $\sum_i \Vdci{i}$ & $n \times 600$\,V \\
$\ig$, $\Ig$          & Grid current (inst.\ / amplitude)   & A; same through all cells \\
$\omg$                 & Grid angular frequency              & $2\pi \times 50$\,rad/s \\
$\phig$                & Grid power factor angle             & Fixed by load \\
$L_f$                  & Grid-side filter inductance         & 1\,mH \\
$\Mi$                  & Modulation index, cell $i$          & $0 \leq M_i \leq 1$; nominal 0.78 \\
$\phic$                & Clamping half-angle (sine conv.)    & 0 (sinusoidal) to $\pi/2$ (full square wave); paper uses $\varphi = 2\phic$ \\
$\PH$                  & Loading power (average per cycle)   & W \\
$\PHi{i}$             & Loading power, cell $i$             & W; equals $P_{i,\mathrm{src}}$ in steady state \\
$\PHnc$                & Non-clamped loading power           & fn.\ of $\Mi$ only \\
$\PHcl$                & Clamped loading power               & fn.\ of $\Mi$ and $\phic$ \\
$P_{i,\mathrm{src}}$  & DC source power, cell $i$           & W \\
$f_{sw}$               & Switching frequency                 & 10\,kHz \\
$f_{bal}$              & Balancing update frequency          & 3.6\,kHz \\
\bottomrule
\end{tabular}
\end{statebox}

\clearpage

% ============================================================
\section{The Control Architecture: Three Loops}
\label{sec:control}
% ============================================================

\textit{This section shows the complete control picture in one place --- the three
nested loops that govern the CHB --- and then explains the conventional PI
balancing method and why it is slow.}

% ── §2.1 ─────────────────────────────────────────────────────────────────
\subsection{The Three-Layer Control Scheme}
\label{subsec:three_loops}

\figentry{fig:paper_fig2}{DLVBMCHBC_Fig2_page02}%
    {Three-layer control scheme for the grid-connected CHB converter
     (paper Fig.\,2).
     \textbf{Outer loop (DC-link voltage control, slow):} compares the
     average DC-link voltage to its reference and generates the grid
     current reference $\ig^{*}$.
     \textbf{Inner loop (grid current control, fast):} forces the actual
     grid current to track $\ig^{*}$ and produces the total voltage
     reference $\vref$.
     \textbf{Balancing layer (per-cell):} divides $\vref$ among the $n$
     cells by computing individual modulation indices $M_i$ (and clamping
     angles $\phic$ in the proposed method).}{0.9}

\begin{circuitbox}[Three-Layer Walk-through (paper Fig.\,2)]
\begin{enumerate}
    \item \textbf{Outer loop --- DC-link voltage control (slow).}
          Measures the average DC-link voltage
          $\bar{V}_{dc} = \frac{1}{n}\sum_i \Vdci{i}$,
          compares it to the reference $V_{dc}^{*}$, and passes the error
          through a PI controller.
          Output: grid current reference $\Ig^{*}$.
          This loop is slow (bandwidth $\approx f_{bal}$) because it
          must not disturb the inner current loop.

    \item \textbf{Inner loop --- grid current control (fast).}
          Measures the actual grid current $\ig$, compares it to $\Ig^{*}$,
          and passes the error through another PI (or resonant) controller.
          Output: total voltage reference $\vref$.
          This loop is fast (bandwidth $\gg f$) to achieve low current THD.

    \item \textbf{Balancing layer --- per-cell modulation.}
          Takes $\vref$ and the measured individual voltages $\Vdci{i}$,
          and computes a per-cell output reference $v_{iH}^{*}$ for each cell.
          In the conventional method this is done with per-cell PI
          controllers (paper Fig.\,3).
          In the proposed method it is done by the sorting algorithm
          with clamped/non-clamped modulation (Sections~\ref{sec:proposed}
          and~\ref{sec:algorithm}).
\end{enumerate}
\end{circuitbox}

The key constraint linking all three layers is the \emph{coupling constraint}:
all cells share the same $\ig$, so their voltage references must sum to $\vref$:

\begin{eqnbox}[Coupling Constraint]
\[
    \sum_{i=1}^{n} v_{iH}^{*} \;=\; \vref
    \qquad
    \Longleftrightarrow
    \qquad
    \sum_{i=1}^{n} \Vdci{i}\,\Mi \;=\; \vref
\]
Any increase in one cell's $\Mi$ must be compensated by decreases in
the others, or the total voltage reference is violated.
This coupling is what makes balancing non-trivial.
\end{eqnbox}

% ── §2.2 ─────────────────────────────────────────────────────────────────
\subsection{Conventional PI Balancing --- The Method This Paper Replaces}
\label{subsec:conventional_pi}

\figentry{fig:paper_fig3}{DLVBMCHBC_Fig3_page02}%
    {Conventional PI-based DC-link voltage balancing (paper Fig.\,3).
     Each cell $i$ has a dedicated PI controller that compares
     $\Vdci{i}$ to the average voltage $\bar{V}_{dc}$ and adds a
     correction $\Delta v_{iH}$ to the shared voltage reference $\vref$,
     giving a per-cell reference $v_{iH}^{*} = \vref + \Delta v_{iH}$.
     All $n$ correction terms must sum to zero to satisfy the coupling
     constraint.}{0.9}

\begin{circuitbox}[How Conventional PI Balancing Works]
Each cell runs an independent PI loop:
\[
    v_{iH}^{*}(k) \;=\; \vref(k) \;+\; \Delta v_{iH}(k)
\]
where $\Delta v_{iH}$ is proportional-plus-integral of the individual
voltage error $(\Vdci{i} - \bar{V}_{dc})$.

The controller must respect the coupling constraint
$\sum_i \Delta v_{iH} = 0$: if one cell's correction increases,
another's must decrease by the same amount.
\end{circuitbox}

\textbf{Why it is slow.}
The PI bandwidth cannot be set freely.
There is a strict hierarchy of time constants:
\[
    t_{sw} \;\ll\; t_{c} \;\ll\; t_{v} \;\ll\; t_{bal}
\]
where $t_{sw}$ is the switching period, $t_c$ the current-loop bandwidth,
$t_v$ the voltage-loop bandwidth, and $t_{bal}$ the balancing settling time.
The balancing loop must be \emph{slower} than the voltage loop to avoid
destabilising it.
This intrinsic hierarchy means the PI controller can only correct a tiny
fraction of the voltage imbalance each grid cycle.
Result: settling time $\approx 1850$\,ms (92 grid cycles at 50\,Hz).

\begin{warningbox}[The fundamental limitation of PI balancing]
The PI adds a small $\Delta v_{iH}$ each cycle.
The maximum step size is limited by the requirement not to distort the
grid current.
No matter how aggressively the gains are tuned, the settling time is
bounded below by the bandwidth hierarchy.
This is the limitation the paper overcomes --- not by better PI tuning,
but by replacing the architecture entirely.
\end{warningbox}

\clearpage

% ============================================================
\section{The Proposed Method: Modulation-Based Balancing}
\label{sec:proposed}
% ============================================================

\textit{This section introduces the paper's key idea: instead of nudging modulation
indices by small increments, shape the modulation waveform itself to access a
wider range of loading power --- then solve directly for the waveform parameters
that achieve exact balance in one balancing period.}

% ── §3.1 ─────────────────────────────────────────────────────────────────
\subsection{The Big Picture: Algorithm Overview}
\label{subsec:algo_overview}

\figentry{fig:paper_fig4}{DLVBMCHBC_Fig4_page03}%
    {Overview of the proposed modulation-based balancing algorithm
     (paper Fig.\,4).
     Each balancing period, the algorithm:
     (1) ranks all $n$ cells by measured DC-link voltage $\Vdci{i}$,
     (2) assigns each cell either \emph{clamped} or \emph{non-clamped}
         modulation based on sign of $\vg\ig$ and rank,
     (3) computes the clamping angle $\phic$ and modulation index $\Mi^{*}$
         to achieve the exact target loading power $\PHi{i} = P_{i,\mathrm{src}}$.
     The sorting and assignment rules are detailed in
     Section~\ref{sec:algorithm}.}{0.9}

The algorithm assigns one of two modulation modes to each cell every
balancing period.
Understanding both modes --- and their different loading power
capabilities --- is the key to understanding the whole paper.

% ── §3.2 ─────────────────────────────────────────────────────────────────
\subsection{Two Modulation Modes}
\label{subsec:two_modes}

\textbf{Non-clamped mode.}
Standard sinusoidal modulation:
\[
    v_{nc}^{*}(\theta) \;=\; \Mi\,\sin(\theta), \quad \theta = \omg t
\]
Amplitude is $\Mi \leq 1$.
The waveform tracks the sinusoid at all times.

\textbf{Clamped mode.}
The reference is held (``clamped'') at $\pm 1$ for an interval of
$2\phic$ centred on each peak, and follows a sinusoid elsewhere:

\begin{eqnbox}[Clamped Reference Waveform (paper Eq.\,2)]
\[
    v_{c}^{*}(\theta) \;=\;
    \begin{cases}
        +1          & \text{if } \sin\theta \geq \cos\phic \text{ (near positive peak)} \\
        \Mi\sin\theta & \text{if } |\sin\theta| < \cos\phic \text{ (middle region)} \\
        -1          & \text{if } \sin\theta \leq -\cos\phic \text{ (near negative peak)}
    \end{cases}
\]
where $\phic \in [0, \pi/2]$ is the \emph{clamping half-angle}
(sine convention used in this document; paper Eq.\,6 uses
$\varphi = 2\phic \in [0,\pi]$, cosine convention).
\end{eqnbox}

Key points about the clamped waveform:
\begin{itemize}
    \item The flat regions are at the \textbf{peaks} ($\pm 1$), not near
          the zero crossings.
          The cell is clamped to maximum output when the grid current
          is largest --- exactly when it can deliver the most power.
    \item The amplitude of the flat region is \textbf{1} (unit),
          independent of $\Mi$.
          This is what gives the clamped cell extra loading power
          beyond what sinusoidal modulation at index $\Mi$ would provide.
    \item At $\phic = 0$: no clamping, standard sinusoid.
          At $\phic = \pi/2$: fully clamped (square wave at $\pm 1$).
\end{itemize}

\figentry{fig:paper_fig7}{DLVBMCHBC_Fig7_page04}%
    {Clamped modulation reference signal with clamping half-angle $\phic$
     (paper Fig.\,7).
     The waveform is held at $\pm 1$ for $2\phic$ centred on each peak.
     Outside the clamped intervals the reference is sinusoidal with
     modulation index $\Mi$.
     The shaded regions show where the clamping is active.
     Increasing $\phic$ extends the clamped interval, reducing $\Mi$
     required to satisfy the coupling constraint and increasing the
     loading power extracted from this cell.}{0.85}

% ── §3.3 ─────────────────────────────────────────────────────────────────
\subsection{Loading Power of Each Mode}
\label{subsec:loading_power_derivation}

\textbf{Non-clamped result.}
With $v_i(t) = \Vdci{i}\,\Mi\,\sin\theta$ and
$\ig(t) = \Ig\,\sin(\theta - \phig)$, the product-to-sum identity
gives immediately:

\begin{eqnbox}[Non-Clamped Loading Power]
\[
    \PHnc \;=\; \frac{1}{2}\,\Vdci{i}\,\Mi\,\Ig\,\cos\phig
\]
The only control variable is $\Mi \in [0,1]$.
Maximum achievable: $\PHnc^{\max} = \frac{1}{2}\,\Vdci{i}\,\Ig\,\cos\phig$
at $\Mi = 1$.
\end{eqnbox}

\textbf{Clamped result.}
The clamped waveform has a piecewise form, so the loading power integral
must be evaluated in pieces.
The calculation splits the period into three intervals: the two clamped
intervals near the peaks, and the sinusoidal middle region.
The result is (paper Eq.\,6):

\begin{eqnbox}[Clamped Loading Power]
\[
    \PHcl \;=\; \frac{\Vdci{i}\,\Ig\,\cos\phig}{2\pi}
    \Bigl[
        \Mi(\pi - 2\phic) - \Mi\sin(2\phic) + 4\sin(\phic)
    \Bigr]
\]
Two free variables: $\Mi$ and $\phic$.
\end{eqnbox}

\textbf{The ratio at full clamping ($\phic = \pi/2$, square wave):}
\[
    \frac{\PHcl}{\PHnc}\bigg|_{\phic = \pi/2}
    \;=\; \frac{4}{\pi \Mi}
\]
At nominal $\Mi = 0.78$: ratio $\approx 1.63$.
At $\Mi = 0.70$: ratio $\approx 1.82$.
The ratio is \textbf{$\Mi$-dependent} --- lower $\Mi$ gives a larger
ratio, which is why the paper's maximum imbalance capability surface
rises at low $\Mi$ values.

\begin{physbox}[Why clamping extracts more power]
In non-clamped mode, the cell voltage near the peaks is $\Vdci{i}\Mi\sin\theta < \Vdci{i}$.
In clamped mode, it is held at $\Vdci{i}$ (the full DC-link voltage).
The grid current $\ig$ is largest near the peaks.
Power = voltage $\times$ current.
By clamping at unit amplitude exactly when the current is high,
the clamped cell delivers significantly more power per cycle than a
purely sinusoidal cell with the same nominal $\Mi$.
\end{physbox}

% ── §3.4 ─────────────────────────────────────────────────────────────────
\subsection{The Loading Power Surface}
\label{subsec:loading_power_surface}

\figentry{fig:paper_fig8}{DLVBMCHBC_Fig8_page04}%
    {Loading power surfaces normalised to the non-clamped base case
     (paper Fig.\,8).
     \textbf{Left (clamped surface):} $\PHcl / \PHnc^{0}$ as a function of
     modulation index $\Mi$ and clamping half-angle $\phic$.
     At small $\phic$ (little clamping) the surface approaches 1.0 p.u.;
     at large $\phic$ and low $\Mi$ it reaches $\approx 1.82$ p.u.
     The ridge at large $\phic$ is where the ratio $4/(\pi\Mi)$ peaks.
     \textbf{Right (non-clamped surface):} $\PHnc / \PHnc^{0}$ as a function
     of $\Mi$ and $\phic$.
     This surface \emph{falls} as $\phic$ increases, reaching $\approx 0.18$
     p.u.\ at $(\Mi=0.7, \phic=\pi/2)$, because when one cell is clamped
     the others must reduce their modulation indices to satisfy the coupling
     constraint --- they give up power to compensate.
     The gap between the two surfaces is the range of power redistribution
     the algorithm exploits.}{0.95}

The key insight from \figref{fig:paper_fig8}:

\begin{statebox}[What the loading power surface means for balancing]
\begin{itemize}
    \item \textbf{Clamped cell} can absorb/deliver up to 1.82$\times$ the
          ``normal'' loading power at full clamping.
    \item \textbf{Non-clamped cells} drop to as low as 0.18$\times$ normal
          power when compensating.
    \item The \textbf{ratio between the two} is what enables rapid balancing:
          the algorithm can move large amounts of power between cells in a
          single balancing period, not just tiny increments.
    \item The conventional PI method is limited to small $\Delta M_i$
          corrections; the proposed method can use the full width of this surface.
\end{itemize}
\end{statebox}

% ── §3.5 ─────────────────────────────────────────────────────────────────
\subsection{Target Modulation Index}
\label{subsec:target_Mi}

Given the closed-form expressions for loading power, the controller can
\emph{invert} them to solve directly for the modulation parameters that
achieve balance.

For a non-clamped cell, setting $\PHnc = P_{i,\mathrm{src}}$ and
solving for $\Mi^{*}$:

\begin{eqnbox}[Target Modulation Index (Non-Clamped)]
\[
    \Mi^{*} \;=\; \frac{2\,P_{i,\mathrm{src}}}{\Vdci{i}\,\Ig\,\cos\phig}
\]
This is exact --- no iteration required.
The controller measures $\Vdci{i}$, $\Ig$, $\cos\phig$, and knows
$P_{i,\mathrm{src}}$ (estimated from DC source measurements).
\end{eqnbox}

For a clamped cell, both $\Mi$ and $\phic$ are free variables.
The algorithm fixes $\Mi$ from the coupling constraint
($\sum_i \Vdci{i}\,\Mi = \vref$) and solves for $\phic$ to hit the
target loading power.
The exact clamped-cell solution is detailed in Section~\ref{sec:algorithm}.

\clearpage

% ============================================================
\section{The Sorting Algorithm}
\label{sec:algorithm}
% ============================================================

\textit{This section shows exactly how the algorithm runs each balancing period:
which cell gets clamped, how the sign of grid power determines the direction,
and how the assignment extends from two cells to $N$ cells.}

% ── §4.1 ─────────────────────────────────────────────────────────────────
\subsection{Sorting Principle and Sign Rule}
\label{subsec:sorting}

At each balancing period, the algorithm:
\begin{enumerate}
    \item Measures all DC-link voltages $\Vdci{i}$.
    \item Computes the sign of the instantaneous grid power $\vg\ig$
          to determine whether the CHB is in rectifier or inverter mode.
    \item Ranks cells by $\Vdci{i}$ and assigns modes based on rank and sign.
\end{enumerate}

\begin{statebox}[Sign Rule for Mode Assignment]
\textbf{Inverter mode} ($\vg\ig > 0$, CHB delivering power to grid):
\begin{itemize}
    \item Cells with \emph{high} $\Vdci{i}$ (overcharged)
          $\to$ \textbf{clamped} (deliver more power, discharge faster).
    \item Cells with \emph{low} $\Vdci{i}$ (undercharged)
          $\to$ \textbf{non-clamped} (deliver less power, recover slower).
\end{itemize}

\textbf{Rectifier mode} ($\vg\ig < 0$, CHB absorbing power from grid):
\begin{itemize}
    \item Cells with \emph{low} $\Vdci{i}$ (undercharged)
          $\to$ \textbf{clamped} (absorb more power, charge faster).
    \item Cells with \emph{high} $\Vdci{i}$ (overcharged)
          $\to$ \textbf{non-clamped} (absorb less power, charge slower).
\end{itemize}

In both cases, the rule drives all voltages toward the average.
\end{statebox}

% ── §4.2 ─────────────────────────────────────────────────────────────────
\subsection{Two-Cell Case Study}
\label{subsec:two_cell}

\figentry{fig:paper_fig5}{DLVBMCHBC_Fig5_page03}%
    {Two-cell case study showing DC-link voltage balancing with
     $t_{bal} = 5$\,ms (paper Fig.\,5).
     The simulation is divided into four regions (I--IV) by the sign of
     $\vg\ig$ and the relative ordering of $\Vdci{1}$ and $\Vdci{2}$.
     \textbf{Region I ($\vg\ig > 0$, $\Vdci{1} > \Vdci{2}$):}
         Cell~1 is clamped (overcharged, discharging fast), cell~2 is
         non-clamped (undercharged, recovering slowly).
     \textbf{Region II ($\vg\ig < 0$, $\Vdci{1} > \Vdci{2}$):}
         Sign reversal --- cell~2 is now clamped to charge faster,
         cell~1 non-clamped.
     \textbf{Regions III--IV:} voltages have crossed;
         assignment swaps to continue driving toward balance.
     The DC-link voltages converge to the common target within
     $\approx 140$\,ms.}{0.9}

Walking through each region of \figref{fig:paper_fig5}:

\begin{circuitbox}[Region-by-Region Analysis (Two-Cell Case)]
\begin{enumerate}
    \item \textbf{Region I} ($\vg\ig > 0$, inverter mode;
          $\Vdci{1} > \Vdci{2}$).
          Cell~1 is overcharged $\Rightarrow$ clamped:
          it delivers extra loading power and discharges toward the
          average.
          Cell~2 is undercharged $\Rightarrow$ non-clamped:
          it delivers less power and its voltage recovers.
          Both voltages move toward each other.

    \item \textbf{Region II} ($\vg\ig < 0$, rectifier mode;
          still $\Vdci{1} > \Vdci{2}$).
          The grid power sign has flipped.
          Now the rule for rectifier mode applies:
          undercharged cell (cell~2) is clamped to absorb more power
          and charge faster; overcharged cell (cell~1) is non-clamped.
          Balancing continues in the same direction.

    \item \textbf{Regions III--IV.}
          Eventually $\Vdci{1}$ and $\Vdci{2}$ cross (or converge close
          enough that the ranking reverses).
          The algorithm detects the new rank and swaps assignments.
          The back-and-forth is fast: the $\approx 140$\,ms settling
          covers roughly 7 grid cycles.
\end{enumerate}
\end{circuitbox}

% ── §4.3 ─────────────────────────────────────────────────────────────────
\subsection{Four-Cell Case: Five Operation Modes}
\label{subsec:four_cell}

\figentry{fig:paper_fig6}{DLVBMCHBC_Fig6_page04}%
    {Five operation modes for the four-cell ($n = 4$) case
     (paper Fig.\,6).
     Each mode is defined by which cell(s) are clamped and which are
     non-clamped.
     \textbf{Mode 1:} all cells non-clamped (balanced steady state).
     \textbf{Mode 2:} one cell clamped, three non-clamped.
     \textbf{Mode 3:} two cells clamped, two non-clamped.
     \textbf{Mode 4:} three cells clamped, one non-clamped.
     \textbf{Mode 5:} all cells clamped (maximum imbalance correction).
     The algorithm selects the mode with the minimum number of clamped
     cells that can still achieve balance, so as to minimise waveform
     distortion.}{0.9}

For $n > 2$ cells the algorithm must decide not only \emph{which}
cells are clamped but also \emph{how many}.
\figref{fig:paper_fig6} shows the five modes possible for $n = 4$:

\begin{statebox}[Mode Selection Rule]
Starting from Mode~1 (all non-clamped), increment the mode
(add one more clamped cell) until the target loading powers
$\PHi{i}^{*} = P_{i,\mathrm{src}}$ for all cells can be satisfied
simultaneously within the coupling constraint
$\sum_i \Vdci{i}\,\Mi = \vref$.

The minimum-mode strategy limits the distortion introduced by clamping
while still achieving full balance each period.
\end{statebox}

\begin{physbox}[General $N$-Cell Summary]
For $N$ cells with loading powers sorted as
$P_{1,\mathrm{src}} \geq P_{2,\mathrm{src}} \geq \cdots \geq P_{N,\mathrm{src}}$
(inverter mode, cells ranked by descending $\Vdci{i}$):
\begin{itemize}
    \item The $k$ most overcharged cells receive clamped modulation.
    \item The remaining $N-k$ cells receive non-clamped modulation.
    \item $k$ is the smallest integer such that the power targets can
          all be met.
    \item At each update, $k$ can be 0 (all sinusoidal) up to $N$ (all clamped).
\end{itemize}
\end{physbox}

\clearpage

% ============================================================
\section{Performance: Simulation and Experiment}
\label{sec:results}
% ============================================================

\textit{This section shows what the proposed method achieves against the
conventional PI method across three metrics: maximum imbalance capability,
transient response, and steady-state waveform quality.}

% ── §5.1 ─────────────────────────────────────────────────────────────────
\subsection{Maximum Loading Power Capability}
\label{subsec:max_capability}

\figentry{fig:paper_fig9}{DLVBMCHBC_Fig9_page04}%
    {Maximum loading power of the proposed method vs.\ conventional
     PI balancing as a function of modulation index $\Mi$
     (paper Fig.\,9).
     At $\Mi = 0.70$ the proposed method achieves $\approx 1.82$ p.u.,
     while the conventional PI is limited to $\approx 1.43$ p.u.\ ---
     a 27\,\% improvement in the maximum imbalance the converter can
     tolerate before losing balance.
     The proposed method's curve follows $4/(\pi\Mi)$ (the full-clamping
     ratio), while the PI method is bounded by its small-correction
     architecture.}{0.85}

The 27\,\% gain in maximum loading power capability has two practical consequences:
\begin{itemize}
    \item \textbf{Wider operating range}: the CHB can stay balanced under
          larger source power differences (e.g., more unequal PV shading).
    \item \textbf{Faster transients}: because the algorithm can command
          a larger power correction per cycle, it converges faster.
\end{itemize}

% ── §5.2 ─────────────────────────────────────────────────────────────────
\subsection{Simulation: Three Power-Factor Cases}
\label{subsec:simulation_pf}

\figentry{fig:paper_fig10}{DLVBMCHBC_Fig10_page05}%
    {Simulation comparison of conventional (left) vs.\ proposed (right)
     balancing for three power-factor cases: unity PF, 0.8 lagging,
     and 0.8 leading (paper Fig.\,10).
     Each sub-plot shows DC-link voltages $\Vdci{1}$ through $\Vdci{6}$
     over time.
     In all three cases, the proposed method reaches balanced steady state
     significantly faster than the conventional PI.
     Power factor affects the loading power magnitude ($\cos\phig$ factor)
     but not the qualitative advantage of the proposed method.}{0.95}

The three power-factor cases confirm that the speed advantage holds
across the full range of practical operating conditions.
The lagging and leading PF cases reduce the absolute loading power
(smaller $\cos\phig$) but the algorithm compensates by selecting larger
$\phic$, staying on the same normalised loading power surface
(paper Fig.\,8).

% ── §5.3 ─────────────────────────────────────────────────────────────────
\subsection{Simulation: Maximum Performance at Unity PF}
\label{subsec:simulation_max}

\figentry{fig:paper_fig11}{DLVBMCHBC_Fig11_page06}%
    {Maximum-performance simulation at unity power factor
     (paper Fig.\,11).
     Shows the DC-link voltages converging from a severe imbalance
     (one cell significantly overcharged, another undercharged) to
     balanced steady state.
     The proposed method drives the voltages to within $\pm 1$\,\% of
     target in $\approx 140$\,ms.
     Also shown: the grid current $\ig$ remains sinusoidal throughout,
     demonstrating that clamped modulation does not compromise current
     quality during transients.}{0.9}

% ── §5.4 ─────────────────────────────────────────────────────────────────
\subsection{Hardware Prototype}
\label{subsec:hardware}

\figentry{fig:paper_fig12}{DLVBMCHBC_Fig12_page06}%
    {Hardware prototype used for experimental validation
     (paper Fig.\,12).
     Three-cell CHB, 230\,V grid, resistor bank load, isolated DC
     sources per cell.
     The converter is rated for 3\,kW total with $\Vdci{i} = 180$\,V
     per cell (540\,V total DC bus).
     The prototype runs at $f_{sw} = 10$\,kHz and $f_{bal} = 3.6$\,kHz.
     All controller algorithms (conventional PI and proposed) are
     implemented on the same DSP platform for a fair comparison.}{0.85}

% ── §5.5 ─────────────────────────────────────────────────────────────────
\subsection{Experimental: Steady-State Results}
\label{subsec:exp_steady}

\figentry{fig:paper_fig13}{DLVBMCHBC_Fig13_page07}%
    {Steady-state experimental results for three loading cases
     (paper Fig.\,13).
     \textbf{Case 1 (balanced):} all cells at equal $\Vdci{i}$; all
     cells run in non-clamped mode; modulation waveforms are sinusoidal.
     \textbf{Case 2 (mild imbalance):} one cell is clamped with small
     $\phic$; the clamped-mode flat top is just visible on the
     oscilloscope trace.
     \textbf{Case 3 (severe imbalance):} one cell is fully clamped
     ($\phic \approx \pi/2$); the square-wave top is clearly visible
     on the clamped cell's modulation signal.
     In all cases the grid current remains sinusoidal (bottom trace).}{0.95}

The steady-state results confirm:
\begin{itemize}
    \item Non-clamped cells produce clean sinusoidal modulation
          (standard PWM appearance).
    \item Clamped cells show the characteristic flat-top waveform whose
          width grows with $\phic$.
    \item Grid current quality (THD) is maintained across all three cases,
          validating that clamped modulation in the balancing layer does not
          degrade the inner current loop.
\end{itemize}

% ── §5.6 ─────────────────────────────────────────────────────────────────
\subsection{Experimental: Dynamic Transient --- The Payoff Figure}
\label{subsec:exp_transient}

\figentry{fig:paper_fig14}{DLVBMCHBC_Fig14_page08}%
    {Dynamic transient comparison: conventional PI (left) vs.\ proposed
     method (right) for a step imbalance in DC source power
     (paper Fig.\,14).
     \textbf{Conventional PI:} settling time $\approx 1850$\,ms
     (92 grid cycles).
     The DC-link voltages drift far apart before the slow PI brings
     them back.
     \textbf{Proposed method:} settling time $\approx 140$\,ms
     (7 grid cycles).
     The voltages converge within the first few cycles.
     This is the $\mathbf{13\times}$ improvement that is the central
     claim of the paper.}{0.95}

\begin{eqnbox}[The Key Result]
\[
    \boxed{
    \text{Conventional PI: } 1850\,\text{ms}
    \quad\longrightarrow\quad
    \text{Proposed method: } 140\,\text{ms}
    \quad\bigl(13\times \text{ faster}\bigr)
    }
\]
Both methods reach the same steady-state outcome:
balanced DC-link voltages and sinusoidal grid current.
The only difference is how fast they get there --- and the difference
is large enough to be the primary engineering contribution of the paper.
\end{eqnbox}

The speed advantage comes entirely from the change in architecture:
instead of nudging $\Mi$ by $\Delta M_i$ each cycle (PI),
the algorithm computes the exact $\Mi^{*}$ and $\phic^{*}$ that
achieve balance and applies them in \emph{one} balancing period.
The loading power closed-form expressions derived in
Section~\ref{sec:proposed} make this one-step inversion possible.

\end{document}
